<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} - {% endif %}{{ podcast.title }}</title>

  {# Meta tags #}
  <meta name="description" content="{{ description or podcast.description }}">
  <meta name="author" content="{{ podcast.author.name }}">

  {# Open Graph #}
  <meta property="og:title" content="{% if title %}{{ title }} - {% endif %}{{ podcast.title }}">
  <meta property="og:description" content="{{ description or podcast.description }}">
  <meta property="og:type" content="website">
  <meta property="og:url" content="{{ page.url | absoluteUrl(podcast.siteUrl) }}">
  <meta property="og:image" content="{{ podcast.image.url | absoluteUrl(podcast.siteUrl) }}">

  {# Twitter Card #}
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:creator" content="{{ podcast.social.twitter }}">

  {# Feeds #}
  <link rel="alternate" type="application/rss+xml" title="{{ podcast.title }}" href="{{ '/feed.xml' | absoluteUrl(podcast.siteUrl) }}">
  <link rel="alternate" type="application/json" title="{{ podcast.title }}" href="{{ '/feed.json' | absoluteUrl(podcast.siteUrl) }}">

  {# Styles #}
  <link rel="stylesheet" href="/assets/styles/main.css">

  {# Favicon #}
  <link rel="icon" href="/favicon.ico" sizes="any">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <link rel="manifest" href="/manifest.webmanifest">

  {# WebC Components #}
  <style webc:keep>{% getBundle "css" %}</style>
  <script webc:keep>{% getBundle "js" %}</script>
</head>
<body>
  <a href="#main" class="skip-link">Skip to main content</a>

  <header class="site-header">
    <nav class="site-nav" aria-label="Main navigation">
      <a href="/" class="site-logo">{{ podcast.title }}</a>
      <ul>
        <li><a href="/episodes">All Episodes</a></li>
        <li><a href="/about">About</a></li>
        <li><a href="/subscribe">Subscribe</a></li>
      </ul>
    </nav>
  </header>

  <main id="main" tabindex="-1">
    {{ content | safe }}
  </main>

  <footer class="site-footer">
    <div class="footer-content">
      <p>&copy; {{ podcast.copyright }}</p>
      <nav aria-label="Footer navigation">
        <ul>
          <li><a href="/privacy">Privacy Policy</a></li>
          <li><a href="/terms">Terms of Use</a></li>
          <li><a href="/feed.xml">RSS Feed</a></li>
        </ul>
      </nav>
    </div>
  </footer>

  {% block jsonLd %}{% endblock %}
</body>
</html>